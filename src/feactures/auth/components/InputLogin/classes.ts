const baseInputClasses = 
'w-full p-3 bg-[#376575] border border-gray-400 text-gray-800 placeholder:text-transparent rounded-lg outline-none'
const disabledInputClasses = 'disabled:bg-gray-200 disabled:text-gray-600'
const readOnlyInputClasses = 'read-only:text-gray-600 read-only:bg-gray-100'

export const inputClasses = `${baseInputClasses} ${disabledInputClasses} ${readOnlyInputClasses}`


//Este código usa clases para definir la apariencia y el comportamiento de los elementos de entrada (inputs) en un formulario o interfaz de usuario.